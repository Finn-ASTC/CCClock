#include "clk_clock.h"

void clk_update_clock(clk_clock* const clk) {
    // todo
}

clk_texture clk_clock_to_texture(const clk_clock* const clk) {
    // todo

    clk_texture res = {};
    return res;
}
